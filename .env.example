# Database connection (SQLite file)
DATABASE_URL="file:./dev.sqlite"

# JWT settings
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_TTL_DAYS=7

# Server
PORT=3000

# CORS (comma-free single origin, or * to allow all)
CORS_ORIGIN=*
